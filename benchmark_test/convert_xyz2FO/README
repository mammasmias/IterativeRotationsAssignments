This directory contains the converter from xyz file format, to the format
specific to fastoverlap input files, which contains nothing but the atomic
coordinates.

TO COMPILE:

  sh compile.sh

TO RUN:

  ./xyz2FO.x < input_file.xyz > output_file
