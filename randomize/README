This directory contains the fortran routine used to randomize a structure. 
It reads an xyz input file containing one structure, and outputs only the
randomized structure.

TO COMPILE:

    sh compile.sh

TO RUN:

    ./randomize.x < input_file.xyz > output_file.xyz


